package com.underroot.common.dto;

public enum MessageType {
    JOIN_DOCUMENT,
    REQUEST_COMPILE,
    PATCH_DOCUMENT,

    DOCUMENT_STATE,
    USER_JOINED,
    USER_LEFT,
    COMPILE_RESULT,
    AUTH_FAILED
}
